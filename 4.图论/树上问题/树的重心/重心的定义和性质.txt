树的重心：（重心不一定只有一个）
1.以某个节点为根时，最大子树的节点数最少，这个节点是重心
2.以某个节点为根时，每棵子树的节点数都不超过所有节点数的一半，这个节点是重心
3.以某个节点为根时，所有节点走向该节点的总边数最少，这个节点是重心

性质：
1.一棵树最多有两个重心，如果有两个重心，则两个重心一定相邻
2.如果树上增加或删除一个叶节点，变化后的重心最多移动一条边
3.如果把两棵树连起来，新树的重心一定在原来两棵树重心的路径上
4.树上的边权如果都为正数，不管边权怎么分布，所有节点走向重心的总距离和最小